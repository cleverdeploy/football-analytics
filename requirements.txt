# runtime (the image ships a pre-built data/football.db)
flask==3.1.3
gunicorn==23.0.0
numpy

# ingest-only deps (not installed in the image; run ingest on the host):
# requests, pandas, beautifulsoup4, lxml
